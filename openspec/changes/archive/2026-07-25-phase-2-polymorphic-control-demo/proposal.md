## Why

The completed Phase 2 implementation needs a bounded change slice for the provider-backed automatic fixture, schema-derived reducer surface, and physical live demonstration. These behaviors prove that mixed capabilities enter and remain observable through the production control path without engine-specific branches or fallback.

## What Changes

- Create every automatic fixture config through the selected installed provider and alternate SoundFont/Braids by discovery order.
- Reject missing providers, provider/registry disagreement, invalid configs, and conversion failures atomically before Patch installation.
- Derive Patch editing and fixed scalar projection from canonical descriptors and common Patch state.
- Exercise every editable instance through the live window/reducer/render path and report alternating engine identity plus bounded audio consequences.
- Preserve the fixed fixture, stable Patch/channel identity, semantic event path, and no-transport boundary.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `automatic-test-midi`: Provider-backed alternating fixture assignment with atomic typed rejection.
- `one-way-parameter-control`: Descriptor-derived Patch editing and fixed engine-scalar projection.
- `live-observable-demo`: Complete mixed-engine live coverage and observable engine identity.

## Impact

This slice affects automatic MIDI fixture initialization, standalone provider ownership, AppState editing targets, StateProjector/StateTree equality, scalar snapshots, live scene coverage, and live reports. It is one non-overlapping slice of the integrated Phase 2 implementation recorded in `DESIGN.md` and the evaluated CUE architecture.
