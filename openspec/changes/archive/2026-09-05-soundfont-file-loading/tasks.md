## 1. Bank and metadata ownership

- [x] 1.1 Load selected SF2 files into separate preset and numeric-bank projections with bounded file admission and library import; prove malformed rejection, non-overwrite, and independent bank audio.
- [x] 1.2 Resolve immutable capability metadata by asset across config construction, projection, and graph validation; prove two Patches retain different preset catalogs and exact candidate deltas.

## 2. Shared browser and persistence

- [x] 2.1 Connect SoundFont File to the shared SF2 page and correlated worker; prove canonical keyboard navigation, cancellation, stale failure, and acknowledgement-only bank/preset commit.
- [x] 2.2 Resolve saved bank metadata before complete session preparation; prove exact bank/preset restore after original source removal and compatibility with bundled-bank sessions.

## 3. Functional acceptance

- [x] 3.1 Run focused SoundFont, Sample, session, and callback proofs plus a native file-page/preset journey; verify audible finite output and explicit failures.
- [x] 3.2 Update DESIGN.md and coherent OpenSpec status; pass formatting, JavaScript syntax, warnings-denied lint, schema/identity checks, and strict OpenSpec validation.
