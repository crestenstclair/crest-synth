## 1. Resolve the authored return dependency

- [x] 1.1 Obtain the explicit Settings return input and destination behavior in normative Figma Page Layout frame `153:184`, then reconcile this change's specs/design/tasks with that exact node and binding. The user selected Shift+Right on 2026-09-06; nodes `155:205`, `155:208`, and `157:200` now specify suspended PATCH/MIXER page and exact valid focus restoration or existing nearest-enabled repair. Direct inspection confirms no unmapped annotation remains. Preserve the existing Shift+Down escape as compatibility behavior.

## 2. Reuse canonical navigation ownership

- [x] 2.1 Add `NavigatePage(Direction)` to the canonical semantic action/event path and implement surface-first reducer dispatch through existing transition/effect helpers. Update action kinds, conversion, admission, and event/schema descriptors. Verify exhaustive compilation/schema checks and a focused reducer test proving one accepted page intent produces one generation and cannot cascade across two edges.
- [x] 2.2 Connect Overview/Detail/Mixer through the existing Detail and context-root helpers, retaining subordinate Choice/file returns and cleanup. Verify normal startup opens Overview; Instrument and duplicate-capability effect slots open the exact subject; empty effects reject unchanged; Detail return and repeated PATCH/MIXER round trips restore exact valid roots or exposed deterministic repair.
- [x] 2.3 Connect Overview Page Left to existing MIDI Settings suspension and discovery effects without changing the selected Patch. Verify temporary entry preserves performance identity through device-list updates, rejects unsafe modal/edit/held-preview states unchanged, and retains compatibility entry and the existing escape alongside the authored return.
- [x] 2.4 Reuse Q/E `SelectPatch` behavior and update affected old Shift+Left navigation assertions. Verify sparse Patch identities, corresponding highlighted control/repair, non-wrapping boundaries, trailing-empty return with Q, and the unchanged 16-Patch capacity with no content or graph mutation from navigation.

## 3. Wire physical inputs and projected guidance

- [x] 3.1 Normalize native arrow keys alongside WASD and map keyboard/controller Shift directions to semantic page intent. Preserve non-conflicting shortcuts, bare focus/edit actions, key-release/focus-loss cleanup, and subordinate controls. Verify translator/native-normalization tests cover arrow/WASD equivalence, Q/E, modifier ordering, repeat/redispatch handling, and one transition per physical activation without preview leakage.
- [x] 3.2 Project source-specific admitted page actions and truthful footer labels for Overview, Detail, Mixer, and Settings; update affected serialized action fixtures. Verify production projection/schema tests show Q/E and the correct page chords, omit unavailable actions, and preserve one canonical focus without browser-owned navigation state.

## 4. Implement the completed Settings return contract

- [x] 4.1 Implement authored Shift+Right / Shift+D Settings return and projected guidance through existing semantic input and Settings restoration, matching Figma nodes `155:205`, `155:208`, and `157:200`. Verify deterministic tests restore suspended PATCH/MIXER identity or the documented repair, preserve discovery/connection ownership, and do not select the next Patch. Retain Shift+Down compatibility, but do not use it as authored return proof.

## 5. Prove and document the complete page graph

- [x] 5.1 Run focused production-path integration coverage across mapped edges, identity repair, admission, and affected Choice/Sample/SoundFont workflows. Verify navigation-only journeys preserve saved-session content, dirty state, subscriptions/routes, active graph identity, and ongoing render behavior; file cancellation still stops its preview. Record exact commands/results and any unresolved Settings-return edge without presenting partial results as phase completion.
- [x] 5.2 Add and run a bounded page-navigation journey in the existing native webview harness with a focused invocation. Verify actual Q/E and Shift+arrow capture, a WASD equivalence check, painted page/subject/focus/return identity, truthful guidance, and closure of the witness-owned window. Final acceptance requires the authored Settings return from 4.1; do not substitute direct reducer injection, a resize sweep, pixel matching, or unrelated soak scenes.
- [x] 5.3 After the full contract and evidence pass, update `DESIGN.md` with the actual bindings, ownership, compatibility behavior, and proof; run required affected-code checks, `openspec validate phase-04-page-navigation --strict`, and `git diff --check`. Verify all completion signals are evidenced and no binding remains unresolved before presenting the change for acceptance and subsequent spec sync/archive.
