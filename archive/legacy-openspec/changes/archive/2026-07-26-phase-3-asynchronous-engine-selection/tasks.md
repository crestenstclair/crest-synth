## 1. Master design and executable architecture (planning complete)

- [x] 1.1 Update `DESIGN.md` with the durable engine-selection lifecycle: descriptor-default target configs, no inactive-engine cache, old graph audible during preparation, commit-before-publication ordering, one structural request in flight, visible typed failure, and the declared complete-graph voice/tail reset boundary.
- [x] 1.2 Update CUE goals, capabilities, and requirements to replace the completed read-only/no-selector guards with the bounded Phase 3 engine-selection behavior while preserving the two-engine, two-context, no-fallback, hard-real-time product boundaries.
- [x] 1.3 Add or amend the canonical CUE Control/Synth resources for engine-selection request identity, lifecycle status, typed failure, descriptor-default config creation, semantic outcomes, reducer invariants, projections, and traceability without duplicating Patch, config, or engine models.
- [x] 1.4 Add or amend the canonical CUE RealTime/Shell resources for the injected capacity-one graph-preparation worker, layout-changing complete graph publication, staged retry, acknowledgement/retirement collection, composition ownership, and control-only shutdown.
- [x] 1.5 Extend the CUE Testing assets, validations, evidence, witness predicates, and project check set with `validation.engine_selection_workflow`, exhaustive headless engine-selection observations and controlled failure proof, plus paced live success in both directions.
- [x] 1.6 Evaluate `openspec context --json` after the CUE edits and verify the rendered project/relationship index contains every new or changed resource, contribution, validation, and witness with no remaining read-only/no-selector contradiction.

## 2. Canonical candidate and reducer lifecycle

- [x] 2.1 Implement one generic descriptor-default config constructor that extracts typed defaults and required default assets in descriptor order, calls the identity-matched capability provider, revalidates through the immutable registry, and rejects every mismatch without SoundFont/Braids branches or fallback.
- [x] 2.2 Add the single canonical engine-selection request id, lifecycle status, and typed preparation-failure values with stable serialized names and production-owned exhaustive descriptors.
- [x] 2.3 Extend the closed semantic event, emitted-effect, event-source, and rejection vocabularies for one worker outcome and one activation acknowledgement, including busy, stale, mismatched, and unavailable cases and the required schema-version migrations.
- [x] 2.4 Implement the PATCH Edit+Left/Right request transition in `AppState::apply`: resolve the adjacent nonwrapping registry choice, enter `Preparing`, advance generation once, emit one correlated preparation request, and preserve the active InstrumentConfig and graph revision.
- [x] 2.5 Implement correlated failure and prepared-result transitions: failure records visible status with exact session/graph preservation; success revalidates and commits only the staged candidate config, enters `Activating`, and preserves Patch identity, MIDI channel, mixer route, ADSR, and every untargeted Patch.
- [x] 2.6 Implement acknowledgement and recovery transitions so only exact active/retired/collected correlation reaches `Ready`, failed status can be replaced by a later request, and early, duplicate, stale, or mismatched events are unchanged nonfatal rejections.
- [x] 2.7 Add reducer and descriptor-table tests for both engine directions, registry boundaries, unavailable PATCH actions, one-in-flight throttling, failure preservation, prepared-result validation, acknowledgement ordering, request-id overflow, later-event recovery, and ordinary MIDI/context/Scalar events during preparation.

## 3. PATCH projection, serialization, and basic input

- [x] 3.1 Give the engine row one stable semantic control identity and initialize PATCH control focus to it without adding a modal, widget index, adapter-owned focus, or another top-level context.
- [x] 3.2 Extend `PatchPageProjection` with exact active/requested capability, request id, lifecycle status, target revision, editability, and typed failure fields while continuing to derive all engine choices and active rows generically from the registry and descriptor.
- [x] 3.3 Extend canonical state serialization, `StateTree`, `StateSnapshot`, `TextProjection`, hashes, leaf descriptors, and schema versions so each request, failure, prepared commit, and acknowledgement generation has exact cross-projection equality.
- [x] 3.4 Update PATCH text rendering to mark only the ready/failed engine row editable, show `Preparing` and `Activating` distinctly, display typed failure without worker strings, and keep MIDI channel, ADSR, Scalar, and Structural rows read-only.
- [x] 3.5 Preserve the existing normalized 17-input keyboard vocabulary and prove K+A/D becomes the PATCH adjacent-choice request only through reducer context/focus, while K+W/S, Navigate, other rows, key-up, and focus loss retain their declared behavior.
- [x] 3.6 Extend headless eframe-context tests to dispatch a real K+A/D chord, render the pending engine row on the next frame, prove the exact event record and scroll target, and confirm the adapter owns no lifecycle or graph state.
- [x] 3.7 Extend eager/deferred projection equality and the 512-event control performance fixture for the larger runtime schema, including MIDI accepted while selection is pending and byte-identical materialization from the same generation.

## 4. Off-callback preparation and structural handoff

- [x] 4.1 Define the replaceable capacity-one graph-preparation worker contract and canonical request/result payloads, keeping `PreparedGraph` ownership outside AppState and submission/polling nonblocking on control ticks.
- [x] 4.2 Implement the production standard-library worker adapter with bounded request/result channels, the injected providers/preparers and negotiated audio configuration, exact request correlation, off-callback candidate destruction, and control-owned shutdown after stream release.
- [x] 4.3 Implement a manually advanced deterministic worker adapter for Testing whose healthy path uses the real production providers, preparers, and `PreparedGraphBuilder` and whose controlled path returns one typed failure without fabricating state or report fields.
- [x] 4.4 Extend complete graph preparation so a candidate may replace one selected capability/layout at the same ordered PatchId, rejects every unrelated identity/order/capacity/route/config difference, and can refresh its embedded fixed initial snapshot from the exact committed completion generation.
- [x] 4.5 Extend `StructuralGraphCoordinator` to stage and retry exactly one complete graph, admit the validated selected-Patch layout change, retain its prior required layout until full acknowledgement, and adopt the new layout only after activation, retirement status, and control-side collection agree.
- [x] 4.6 Add worker/builder/coordinator tests for both real engine directions, Scalar edits accepted during preparation, incompatible candidates, queue pressure, staged retry, one-in-flight throttling, stale revisions, full retirement transport, off-callback drops, and layout adoption after acknowledgement.
- [x] 4.7 Preserve and remeasure the callback contract during layout-changing swaps: block-boundary activation, compatible snapshot selection, bounded command routing, finite output, and zero allocation, deallocation, locking, blocking, I/O, logging, panic, or destruction.

## 5. Application orchestration and composition

- [x] 5.1 Inject the worker control and structural-control boundaries into `AppLoop` (or its one owning application coordinator) so UI/testing adapters retain only semantic dispatch and immutable projection access.
- [x] 5.2 Wire accepted engine-request effects to one worker submission while keeping the current graph rendering and allowing MIDI, context selection, and valid MIXER Scalar edits to continue through their normal paths.
- [x] 5.3 Handle a healthy worker result in strict order: correlate and stage the graph, apply the semantic prepared result, commit candidate state, derive/publish the target-revision projections and refreshed initial snapshot, then transfer complete graph ownership and record the structural effect.
- [x] 5.4 Handle worker failure, unavailable publication, and mismatched results without rollback or fallback: retain old state for failure, retain one staged graph for publication retry after commit, destroy rejected candidates only off callback, and keep later valid events processable.
- [x] 5.5 Poll graph status and retirement on control ticks, collect every returned graph outside the callback, dispatch the exact acknowledgement event once, update target revision/layout, and admit the next request only after the lifecycle is `Ready` or recoverable `Failed`.
- [x] 5.6 Extend `EventRecord`/`EventLog` observations to distinguish user request, worker outcome, parameter publication, staged/published graph effect, and activation acknowledgement with exact request ids, generations, hashes, revisions, and before/after values.
- [x] 5.7 Update normal, smoke, headless-demo, and live-demo composition to inject the same providers, preparers, worker port, structural boundary, audio observation, and negotiated configuration with no hidden concrete worker/boundary or duplicate engine-selection path.
- [x] 5.8 Prove normal shutdown, early error, demo completion, and live-demo completion release the audio stream before joining/draining worker ownership and never destroy a prepared or retired graph on the callback.

## 6. Headless and live demo-scene proof

- [x] 6.1 Extend `DemoScene`, its step vocabulary, production-derived coverage descriptor, report/checkpoint schema, and event-log capacity for explicit worker advancement, graph activation/acknowledgement, lifecycle states, structural effects, and typed failures.
- [x] 6.2 Add the focused first-Patch SoundFont-to-Braids scene path through Digit2 and K+D, checkpoint the old engine while `Preparing`, exercise the busy rejection, advance real preparation, render/acknowledge the swap, send targeted MIDI, and measure finite nonzero Braids output at the same PatchId/stem.
- [x] 6.3 Add the Braids-to-SoundFont reverse path through K+A, require the exact descriptor-default SoundFont config and HiDef asset, acknowledge the new revision, measure finite nonzero SoundFont output, and declare that exact final structural config.
- [x] 6.4 Add one controlled worker preparation failure plus stale/mismatched outcome and acknowledgement probes; prove config/revision/layout/audio preservation, no publication or fallback, unchanged rejected generations/hashes, and successful later recovery.
- [x] 6.5 Compare every lifecycle checkpoint across canonical state, Patch page, text, StateTree, EventRecord, target-revision ParameterSnapshot, structural status, prepared slot identity, routed stems, and callback counters; reject labels, construction, or master-buffer change as sufficient evidence.
- [x] 6.6 Preserve exact restoration for Scalar parameters, sends, MIXER selection, and context; require the declared descriptor-default SoundFont final config and byte-identical logical event/state/coverage/checkpoint/report output across two fresh `make demo` runs.
- [x] 6.7 Extend `LiveDemoScene`, checkpoints, report, and coverage with a separate ordered engine-transition set. After frozen scalar coverage, request the focused first Patch SoundFont-to-Braids and Braids-to-descriptor-default-SoundFont only through semantic events; hold each step through canonical Preparing/Activating/Ready, require increasing acknowledged revisions, dispatch targeted MIDI, and record finite nonzero target output before proceeding.
- [x] 6.8 Run the physical live composition through `ThreadedGraphPreparationWorker` and the existing nonblocking window tick/`AppLoop.advanceStructural` path; retain mapped-input isolation, scalar pacing, semantic cleanup, exact final SoundFont config, zero fallback/callback allocation/destruction, one report/close, and control-owned teardown. Keep failure/stale injection exclusively in the deterministic headless scene.

## 7. Focused acceptance and regression gates

- [x] 7.1 Add `tests/engine_selection_workflow.rs` using production providers/preparers, reducer/projector/AppLoop, worker port, structural boundary, renderer, and audio observations; emit one structured `CREST_ENGINE_SELECTION_OBSERVATION` and `CREST_ACCEPTANCE engine_selection_workflow passed` only after every declared predicate passes.
- [x] 7.2 Add controlled focused cases proving claimed success with the old engine, silent/non-finite target output, fallback on failure, missing publication/acknowledgement, callback destruction, or cross-Patch config leakage cannot emit the engine-selection acceptance marker.
- [x] 7.3 Update capability-schema, PATCH projection, schema-surface, eframe-context, prepared-rack, production-runtime, real-time, control-performance, exhaustive-demo, live-demo, smoke, and mutation fixtures for the exact versioned lifecycle surface; the live contract test must exercise both worker/structural directions and cannot pass on idle-worker compatibility.
- [x] 7.4 Run focused unit suites for capability defaults, request/status/failure types, AppEvent/EventRecord descriptors, AppState transitions, projections/serialization, worker, prepared graph builder, structural coordinator, AppLoop orchestration, keyboard input, text rendering, and deterministic demo steps.
- [x] 7.5 Run `cargo test --test engine_selection_workflow -- --nocapture`, `cargo test --test patch_page_projection -- --nocapture`, `cargo test --test prepared_engine_rack -- --nocapture`, `cargo test --test exhaustive_demo_scene -- --nocapture`, `cargo test --test schema_surface -- --nocapture`, `cargo test --test eframe_context -- --nocapture`, `cargo test --test live_demo_scene -- --nocapture`, `cargo test --test production_runtime_contracts -- --nocapture`, and `cargo test --test control_dispatch_performance -- --nocapture` with every declared marker and structured predicate.

## 8. Full verification and roadmap checkpoint

- [x] 8.1 Run `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all-targets`, and `make smoke` with no ignored failure or zero-test selector.
- [x] 8.2 Run two fresh `make demo` executions and require byte-identical logical `CREST_EVENT_LOG`, `CREST_STATE_TREE`, coverage, engine-selection checkpoints, and observation evidence after excluding no declared field.
- [x] 8.3 Run one complete `make demo-live` on a usable 48 kHz physical output device and require visible Preparing/Activating/Ready checkpoints plus audible finite SoundFont-to-Braids and Braids-to-descriptor-default-SoundFont results, exact transition coverage, final Ready SoundFont state, semantic note cleanup, one close, stream release, and exit zero.
- [x] 8.4 Run every CUE-declared project validation and witness, including the new engine-selection contract and all controlled negatives, and require zero callback allocation/destruction during both layout-changing swaps.
- [x] 8.5 Run `openspec context --json`, require a healthy `openspec doctor --json`, run `openspec validate phase-3-asynchronous-engine-selection --strict` and `openspec validate --all --strict`, then run deterministic change/project acceptance for the complete updated project-check set.
- [x] 8.6 After all acceptance passes, update `ROADMAP.md` to record the read-only PATCH projection and asynchronous engine-selection increments as complete and identify PATCH ADSR editing as the next Phase 3 increment without expanding its implementation scope.
